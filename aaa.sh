#!/usr/bin/env bash

# aws dynamodb --region local --endpoint-url http://localhost:8000 list-tables

#aws dynamodb --region local --endpoint-url http://localhost:8000 scan --table-name PinterestAdGroup

jaws dynamodb scan --table-name PinterestAdGroup